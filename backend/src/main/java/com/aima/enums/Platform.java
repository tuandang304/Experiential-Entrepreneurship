package com.aima.enums;

/**
 * Social platforms in current scope (FR-14). TikTok / YouTube Shorts / LinkedIn
 * are intentionally excluded per REQUIREMENTS.md.
 */
public enum Platform {
    FACEBOOK,
    INSTAGRAM,
    THREADS
}
