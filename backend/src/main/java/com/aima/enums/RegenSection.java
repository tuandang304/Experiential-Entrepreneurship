package com.aima.enums;

/**
 * Phần kịch bản video được chọn để tạo lại từng phần (tạo lại độc lập, không đụng phần khác):
 * HOOK = Mở đầu, BODY = Nội dung chính (các bước), CTA = Kết bài.
 */
public enum RegenSection {
    HOOK,
    BODY,
    CTA
}
